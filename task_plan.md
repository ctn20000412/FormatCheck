# 任务计划：单机版论文格式检查系统开发文档

## 目标

为当前项目补齐单机版开发文档，明确“模型选择 + 规范文件上传 + 待检论文上传 + Agent1 规则抽取 + Agent2 格式检查 + 本地文件生成 + 前端展示下载”的实现边界和开发依据。

## 范围

- 只考虑单机版本地运行。
- 不设计数据库、账号体系、网络上线、云端部署、多用户协作。
- 文件保存在本地项目目录。
- 两个核心 skill 已存在：
  - `skills/extract-format-rules`
  - `skills/check-paper-format`

## 阶段

| 阶段 | 状态 | 产物 |
| --- | --- | --- |
| 1. 梳理项目理解和约束 | complete | `findings.md` |
| 2. 创建规划跟踪文件 | complete | `task_plan.md`、`findings.md`、`progress.md` |
| 3. 编写开发文档 | complete | `docs/*.md` |
| 4. 检查文档一致性 | complete | 完整性检查记录 |

## 待创建文档

| 文档 | 目的 |
| --- | --- |
| `docs/单机版需求说明.md` | 定义用户目标、输入输出、功能边界 |
| `docs/系统架构设计.md` | 定义模块、职责和本地系统结构 |
| `docs/流程设计.md` | 定义端到端业务流程和异常流程 |
| `docs/数据结构说明.md` | 定义 JSON、文件元信息、前端展示数据 |
| `docs/前端页面与交互说明.md` | 定义页面区域、状态、下载入口 |
| `docs/Agent与Prompt说明.md` | 定义 Agent1/Agent2、prompt、skill 调用方式 |
| `docs/本地文件目录规范.md` | 定义输入、输出、临时文件保存规则 |
| `docs/测试用例.md` | 定义开发测试场景 |
| `docs/开发文档索引.md` | 汇总开发文档入口和阅读顺序 |

## 决策

- 结果文件以待检测文档名目录隔离。
- `format_rule.json` 是 Agent1 的正式输出，也是 Agent2 的输入。
- Agent2 必须输出错误统计分析文档和批注版论文。
- 前端展示错误统计数据，同时提供结果文件下载入口。

## 补充决策

- 前端采用 Vue。
- 接口层采用 Java Spring Boot。
- LLM 交互和 Agent 执行采用 Python。
- Java 负责接口，Python 负责调用外部 LLM API。
- 支持模型厂商：`deepseek`、`chatgpt`、`claudecode`、`kimi`、`glm`。
- API Key 和 Base URL 只在 Python 配置中维护。
- 规范文件支持 `.pdf`、`.doc`、`.docx`、`.md`。
- 待检测论文当前只支持 `.docx`。
- 格式检查优先使用程序读取 Word 属性。
- 错别字和语法错误由 LLM 判断。
- 分析文档输出 `.docx`。
- 批注版论文必须使用 Word 原生批注。
- 结果目录按待检测文档名命名，保留，不自动清理。
- 结果目录内部按五类业务目录保存：`待检测文件`、`规则规范文件`、`规则抽取结果`、`检测结果`、`检测后带批注的源文件`。
- Java 启动 Python 的方式固定为 `ProcessBuilder` 启动脚本。

## 一致性检查

- 未发现未完成标记。
- 文档边界保持为单机版，不引入数据库、账号、多用户或部署方案。
- 已创建文档索引。
- 已完成最终文件清单检查。
