# 2026-05-01 系统完成度检查发现

## 验证结果

- Python Agent 单元测试通过：`Ran 17 tests, OK (skipped=1)`。
- Java 后端测试通过：`Tests run: 7, Failures: 0, Errors: 0, Skipped: 0`。
- Vue 前端生产构建通过：`vite build` 成功。

## 主要完成项

- 前端具备模型选择、文件上传、结果展示和下载入口。
- Java 后端具备文件校验、结果目录创建、模型校验、HTTP 调用 Python Agent。
- Python Agent 已常驻服务化，支持流式 LLM 调用和终端日志。
- DOCX 证据提取已升级为 `python-docx+xml`，可读取 WordprocessingML 结构化格式信息。
- 已支持原生 Word 批注输出。

## 主要风险

- 大论文整篇一次传给 LLM，存在上下文长度、费用和响应时间风险。
- PDF/DOC 规范文件解析还不够成熟，尚未完整接入 `pdfplumber`、`pypdf`、Poppler、LibreOffice 转换链路。
- 当前同步 HTTP 任务容易在长耗时模型调用中超时。
- 规则抽取结果缺少强归一化，模型输出不稳定会影响 Agent2 检查准确率。
- 终端完整打印 prompt 和长文本，调试有用但会拖慢大任务并污染日志。

## 优先建议

1. 异步任务化：提交任务返回 `taskId`，前端轮询状态。
2. 程序确定性检查：字体、字号、页边距、行距等由 Python 直接判断。
3. 规则标准化：Agent1 输出固定可执行字段。
4. 规范文件解析增强：补齐 PDF/DOC 成熟读取链路。
5. 批注定位增强：支持 run、表格单元格、页眉页脚和题注。
