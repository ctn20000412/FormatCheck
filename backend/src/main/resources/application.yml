server:
  port: 8080

spring:
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 200MB

format-check:
  storage-root: ../results
  model-config: ../agent-service/config/llm_providers.json
  agent-base-url: http://127.0.0.1:8001
  agent-timeout-seconds: 300
